name: RivalDraft
version: '${project.version}'
main: mc.sseakk.rivaldraft.RivalDraft
api-version: '1.20'
prefix: RivalDraft
softdepend: [ Duels ]
authors: [ sSeaKk ]
description: Tournament generation plugin

commands:
  test:
      description: test command
  rivaldraft:
      description: main command